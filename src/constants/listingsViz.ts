/**
 * Max number of comparable listings drawn as scatter points and map markers.
 * Aggregates (KPIs, regression line, histogram) still use the full filtered set.
 */
export const LISTINGS_VIZ_CAP = 150;
